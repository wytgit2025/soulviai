# soulviai · 核心依赖（对话 / 状态 / 待发队列必需）
#
# 安装：python3 scripts/soulviaictl.py setup --minimal
#   （此命令装的正是下面这几项，无需手动 pip）
#
# 语义向量记忆（fastembed / onnxruntime / numpy，约 300MB）见
# requirements-vector.txt —— 默认不装，缺了引擎会自动降级，对话不受影响。

openai>=1.30.0
httpx>=0.27.0
requests>=2.30.0
python-dotenv>=1.0.0
websocket-client>=1.6.0

# soul-skill · 环境变量模板
#
# 使用方式：
#   cp .env.example .env    然后填入自己的密钥
#
# ⚠️ .env 是敏感文件：
#   - 不要提交到版本库（已在 .gitignore 中）
#   - 不要分发给别人（打包脚本会按 *.env 规则自动剔除）
#
# 本文件中的环境变量优先级高于 config.json 的同名字段。

# ── 模型接口（必填）──
#
# 引擎走 OpenAI 兼容协议（/v1/chat/completions），任意兼容厂商都能用。
# 最省事的填法——只改这两行：
AI_PROVIDER=deepseek
AI_API_KEY=sk-在这里填你的密钥
#
# AI_PROVIDER 会自动补全 api_base 与 model。可选值：
#   openai     deepseek   moonshot   zhipu      dashscope   volces
#   siliconflow  openrouter  groq    together   fireworks   mistral
#   xai        ollama     vllm       lmstudio   llamacpp
# 每个预设对应的默认 base_url 与模型名见 engine/core/ai.py 的 PROVIDERS 表。
#
# 若你的服务不在上表内，就别填 provider，直接写三个值：
#   AI_API_KEY=sk-xxx
#   AI_API_BASE=https://your-endpoint/v1
#   AI_MODEL=your-model-name
#
# 本地模型（Ollama / vLLM / LM Studio）不校验 key，随便填个非空值即可：
#   AI_PROVIDER=ollama
#   AI_API_KEY=ollama
#
# 旧版变量名仍然兼容（优先级低于上面的 AI_*）：
#   DEEPSEEK_API_KEY / DEEPSEEK_API_BASE / DEEPSEEK_MODEL

# ── 托管式宿主 / 云端：靠「注入」，不靠「读配置」──
# 有的宿主本地压根不存模型配置，autoconfig 读无可读，只能由部署方注入：
#   · WorkBuddy：托管式产品，模型走云侧登录鉴权。~/.workbuddy 下没有
#     openclaw.json，models.json 也是空的 —— 没有任何模型接口可探测。
#   · 云端沙箱（CloudStudio 等）：容器里既没有 ~/.qclaw / ~/.workbuddy，
#     OpenClaw 网关的 127.0.0.1:18789 也没人监听。
# 这两种情况在上层显式注入即可（容器 / 平台环境变量 / systemd Environment=）：
#   -e AI_PROVIDER=deepseek -e AI_API_KEY=sk-xxx
#
# 注意：**注入的环境变量优先于 .env 文件**里的同名字段，文件覆盖不了它。
# 这条规则是有意的 —— 部署方的注入应当能压过镜像里带的模板值。

# ── 改完之后怎么生效 ──
# 常驻服务只在启动时读一次配置。要让运行中的服务当场用上新值，执行：
#   python3 scripts/soulctl.py reload-ai
# 否则就是「配置明明改了、服务照样报 401」—— 得重启才生效。

# ── 微信 iLink Bot（可选，仅 run_wx_bot 模式需要）──
# WX_BOT_TOKEN=
# WX_ILINK_BOT_ID=
# WX_ILINK_USER_ID=

# ── 天气 / 定位（无需任何配置）──
# 引擎自己不上网：不查天气、不做 IP 定位。环境信息由调用方通过
#   python3 scripts/soulctl.py chat --text "..." --env "上海 小雨 24°C"
# 注入，随对话即时生效。因此这里不需要任何天气或定位的 Key。
